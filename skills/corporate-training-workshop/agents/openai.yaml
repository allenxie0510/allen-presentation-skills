interface:
  display_name: "Corporate Training Workshop"
  short_description: "Applied corporate workshop web decks."
  default_prompt: "Use $corporate-training-workshop to diagnose my performance need, then either generate with an existing theme, run a three-route Allen FRAME exploration, or test a candidate direction against a real project according to my explicit request."
policy:
  allow_implicit_invocation: true
