interface:
  display_name: "Professional Presentation Core"
  short_description: "Plan and render scenario-aware professional web presentations."
  default_prompt: "Use $presentation-core to classify my presentation scenario, show the recommended real-render theme previews, then turn my brief into a validated, assertion-led Presentation IR and self-contained themed HTML deck."
policy:
  allow_implicit_invocation: true
