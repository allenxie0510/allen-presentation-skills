interface:
  display_name: "Professional Presentation Core"
  short_description: "Plan and render professional HTML or native PPTX decks."
  default_prompt: "Use $presentation-core 0.5.1 to classify my presentation scenario and output format, turn my brief into a validated assertion-led Presentation IR, and render either a self-contained themed HTML deck or a native editable PPTX with slot-aware imagery, rhythm, and visual-QA gates."
policy:
  allow_implicit_invocation: true
